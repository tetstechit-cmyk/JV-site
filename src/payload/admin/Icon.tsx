/* Ícone pequeno no topo da navegação do painel. */
export default function Icon() {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/brand/logo-jv-trim.png"
      alt="João Vitor"
      style={{ height: 26, width: "auto" }}
    />
  );
}
